#!/usr/bin/perl

use strict;
use warnings;
use IO::Dir;
use IO::File;
use IO::Handle;
use IPC::Cmd;
use PerlIO;

# what this list
my ($cat, $dog, $narwhal) = shift;

# what is it name
my @animal = ($cat, $dog, $narwhal);

# what is numered the order ?
$cat = 0;
$dog = 2;
$narwhal = 4;

# each element has an ordered numerical index or
# position
if ($cat == 0) {
    # select model type needs local to settings or homes
    say FILEHANDLE "verify if $cat this needs of inital times!";
    say FILEHANDLE "verify if $cat this needs of inital foods!";
    say FILEHANDLE "verify if $cat this needs of inital send!";
    say LIST "verify the $cat this needs of inital forgot!";
    say LIST "verify the $cat this needs of inital type corns!";
    say LIST "verify the $cat this needs of inital np:)";
    # Each element of an array has to be a scalar
    # variable
    for (my $cat = 0; $cat < 1..512; $cat++) {
         scalar($cat->{"times!"});
         scalar($cat->{"foods!"});
         scalar($cat->{"send!"});
         scalar($cat->{"forgot!"});
         scalar($cat->{"corns!"});
         scalar($cat->{"np:)"});
    }
    # These are all scalar variables!
    for (my $cat = 0; $cat < 1..512; $cat++) {
         scalar($cat->{512*10});
         scalar($cat->{512*20});
         scalar($cat->{512*30});
         scalar($cat->{512*40});
         scalar($cat->{512*50});
         scalar($cat->{512*60});
    }
    
    # number!
    my $number = shift;
    foreach ($number eq $cat->{512*10} lt scalar(1..512)) {
        ## write list handle in scalar
        scalar($number->{$cat}) if scalar(1..512);
        # checkup to analysis of cat ...
        for ($cat = 0; $cat < 1..512; $cat++) {
             scalar($number->{$cat*10}) if scalar(1..512);
             scalar($number->{$cat*20}) if scalar(1..512);
             scalar($number->{$cat*30}) if scalar(1..512);
             scalar($number->{$cat*40}) if scalar(1..512);
             scalar($number->{$cat*50}) if scalar(1..512);
             scalar($number->{$cat*60}) if scalar(1..512);
             # verify the handle list files
             say FILEHANDLE "then this and the $number of $cat*10 list handle";
             say FILEHANDLE "then this and the $number of $cat*20 list handle";
             say FILEHANDLE "then this and the $number of $cat*30 list handle";
             say FILEHANDLE "then this and the $number of $cat*40 list handle";
             say FILEHANDLE "then this and the $number of $cat*50 list handle";
             say FILEHANDLE "then this and the $number of $cat*60 list handle";
             
        }
      return $number->{$cat->{scalar(1..512)}};  
    }
    
  
  # letter!
  if ($dog->{$cat} eq scalar(1..512) lt $cat*10) {
      my $chekup = "bash --dump-po-strings"; # ok success this and one boot start annotation of support
      my $boot = "bash --init-file"; 
      
  }
    
    
}
